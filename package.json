{
  "type": "module",
  "scripts": {
    "ts": "node --no-warnings --experimental-strip-types",
    "setup-tunnel": "pnpm run ts scripts/setup-cf-tunnel.ts",
    "list-tables": "pnpm run ts scripts/list-tables.ts",
    "import-dump": "pnpm run ts scripts/import-dump.ts",
    "merge-dump": "pnpm run ts scripts/merge-dump.ts",
    "test-events": "pnpm run ts scripts/test-events.ts",
    "sync-upstream": "pnpm run ts scripts/sync-from-upstream.ts",
    "start": "docker compose up -d --no-recreate",
    "stop": "docker compose down",
    "logs": "docker compose logs -f"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "cloudflared": "^0.7.0",
    "dotenv": "^16.5.0",
    "postgres": "^3.4.7"
  },
  "engines": {
    "node": ">=22.6.0",
    "pnpm": ">=3"
  }
}